version: 2.1
executors:
  node:
    docker: 
      - image: circleci/node:12.14.1

jobs:
  test:
    executor: node
    working_directory: ~/circleci
    steps:
      - run:
          name: Test
          command: |
            ls -la
            npm run test
  build:
    executor: node
    working_directory: ~/circleci
    steps:
      - run:
          name: Build
          command: |
            npm run build

workflows:
  "circleci":
    jobs:
      - test
      - build
